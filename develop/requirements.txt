# MARS Requirements
# Python 3.11 with CUDA 12.1 support

# PyTorch with CUDA support (install with: --index-url https://download.pytorch.org/whl/cu121)
# Note: PyTorch is installed separately in Dockerfile due to custom index URL

# Physics Engine
pybullet==3.2.5

# Computer Vision and Image Processing
opencv-python==4.8.1.78
opencv-contrib-python==4.8.1.78
pillow==10.1.0
imagehash==4.3.1
scikit-image==0.22.0
supervision==0.18.0

# 3D Processing Libraries
trimesh==4.0.8
# open3d==0.18.0  # Installed separately in Dockerfile before requirements
pymeshlab==2023.12
pywavefront==1.3.3
python-fcl==0.7.0.5
scipy==1.11.4
scikit-learn==1.3.2

# SAM & SAM 3D Objects Dependencies
diffusers==0.25.0
# accelerate is installed later with >=0.26.0 for Qwen2.5-VL compatibility
omegaconf==2.3.0
hydra-core==1.3.2
einops>=0.7.0,<0.9.0  # sam3 requires >=0.8.0, but we keep compatibility
imageio==2.33.1
plyfile==1.0.3

# Workflow Orchestration
prefect>=3.6.4

# Data Processing and Storage
numpy<2.0,>=1.26.0
pandas==2.1.4
h5py==3.10.0
boto3==1.34.21
psycopg2-binary==2.9.9
sqlalchemy==2.0.25

# ML Utilities
transformers==4.49.0  # Florence-2 compatible, SAM 2 support
accelerate>=0.26.0    # Required for Qwen2-VL device_map (conflicts with 0.25.0, so use >=0.26.0)
huggingface-hub[cli]<1.0
clip-interrogator==0.6.0

# General Utilities
pyyaml==6.0.1
tqdm==4.66.1
click==8.1.7
python-dotenv==1.0.0
colorlog==6.8.0
rich>=13.9.4

# USD (Universal Scene Description) Support
usd-core==24.11
